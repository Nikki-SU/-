# 选择题题库格式规范

本文档定义了刷题 App 所支持的选择题题库 Markdown 文件格式。请严格按照以下规则进行排版。

---

## 一、基本规则

1. **文件格式**：`.md`（Markdown 文件）
2. **题目分隔**：每道题之间用独立一行的 `---` 分隔
3. **编码**：UTF-8

---

## 二、单道题结构

一道完整的题目由以下 5 个部分组成，顺序固定：

```
# 题目标题
题干内容（支持 Markdown 语法）
A. 选项A内容
B. 选项B内容
C. 选项C内容
D. 选项D内容
答案：X
解析：解析内容
```

### 各部分详细说明

| 序号 | 部分 | 格式要求 | 是否必填 |
|:---:|------|----------|:--------:|
| ① | **标题** | 以 `# ` 开头，如 `# 第1题` | 建议填写（不填将自动生成） |
| ② | **题干** | 标题之后、第一个选项之前的所有内容 | 必填 |
| ③ | **选项** | 字母 `A` ~ `F` + `.` + 空格 + 选项内容 | 必填，2~6 个 |
| ④ | **答案** | `答案：` 或 `答案:` + 字母 | 必填 |
| ⑤ | **解析** | `解析：` 或 `解析:` + 文字内容 | 建议填写（不填显示"暂无解析"） |

---

## 三、单选 vs 多选

- **单选题**：答案只有 1 个字母，如 `答案：A`
- **多选题**：答案有 2 个及以上字母，字母间**无分隔符**，如 `答案：ACE`

| 题目类型 | 答案示例 | 说明 |
|----------|----------|------|
| 单选 | `答案：B` | 仅一个正确选项 |
| 多选 | `答案：ABD` | 多个正确选项，字母直接连写 |

---

## 四、完整示例

以下是两道题的完整排版示例，涵盖单选和多选：

```markdown
# 第1题
以下哪个是中国的首都是？
A. 上海
B. 北京
C. 广州
D. 深圳
答案：B
解析：北京是中华人民共和国的首都。

---

# 第2题
以下哪些是编程语言？
A. Python
B. JavaScript
C. HTML
D. CSS
E. Java
答案：ABE
解析：HTML 和 CSS 是标记语言/样式语言，不属于编程语言。
```

---

## 五、注意事项

### 1. 选项格式
- ✅ 正确：`A. 选项内容`（字母 + 英文点号 + 空格 + 内容）
- ❌ 错误：`A、选项内容`（使用了中文顿号）
- ❌ 错误：`A.选项内容`（缺少空格）
- ❌ 错误：`1. 选项内容`（使用了数字编号）

### 2. 答案格式
- ✅ 正确：`答案：A`、`答案：ACE`
- ✅ 正确：`答案:A`（英文冒号也可）
- ❌ 错误：`正确答案：A`（不识别"正确答案"前缀）
- ❌ 错误：`答案：A,C,E`（多选题答案不要用逗号分隔）
- ❌ 错误：`答案：a`（必须大写）

### 3. 题干格式
- 支持 Markdown 语法：**加粗**、*斜体*、`行内代码`
- 支持嵌入图片：`![图片描述](图片URL或base64)`
- 支持多段落

### 4. 分隔符
- `---` 必须单独成行，前后各空一行
- 题目内部不要出现 `---`

### 5. 选项数量
- 建议 2~6 个选项（A ~ F）
- 不支持超过 6 个选项

---

## 六、快速模板

复制以下模板，按此格式填充即可：

```markdown
# 第1题
题干内容写在这里……
A. 选项A
B. 选项B
C. 选项C
D. 选项D
答案：A
解析：解析内容写在这里……
```

多道题时，用 `---` 分隔继续添加：

```markdown
# 第1题
题干……
A. 选项A
B. 选项B
C. 选项C
D. 选项D
答案：A
解析：……

---

# 第2题
题干……
A. 选项A
B. 选项B
C. 选项C
D. 选项D
E. 选项E
答案：BCE
解析：……

---

# 第3题
……
```
